# Plan 03-01: Page-Specific Fixes

**Phase:** 3 — Page-Specific Fixes
**Goal:** Fix three discrete UI problems: season2 placeholder artifact, FAQ dead-end contact, progress bar accessibility gap
**Created:** 2026-03-31

## Requirements Covered

| Req ID | Description |
|--------|-------------|
| EXP-01 | Season2 page shows clean "coming soon" message instead of raw HTML placeholder text |
| EXP-02 | FAQ contact block includes an actionable contact method |
| EXP-05 | Session progress bar links have accessible labels (aria-label) |

## Wave 1: All Three Fixes (Independent)

### Task 1: EXP-01 — Replace Season2 Placeholder Artifact
**File:** `src/pages/season2.astro`
**Action:** Replace the inner dashed-border code-display box (lines 142–149) with a single clean status line
**Before:** `<div class="bg-bg-hover/50 ... border-dashed ...">` containing escaped HTML tags
**After:** `<p class="font-mono text-text-muted text-xs">Season 1 진행 중 오픈 예정입니다.</p>`
**Keep:** Outer card structure (? icon, heading, description paragraphs)
**Commit:** `style: EXP-01 -- replace season2 developer artifact with clean status text`

### Task 2: EXP-02 — Add FAQ Contact Link
**File:** `src/pages/faq.astro`
**Action:** Add a `mailto:` link below the existing contact card prose (after line 174)
**Contact value:** `[CONTACT_EMAIL]` placeholder — user replaces before deploy
**Link style:** `font-mono text-accent hover:text-accent-hover text-xs transition-colors`
**Add `mb-3` to existing second `<p>` to space it from the new link**
**Commit:** `style: EXP-02 -- add contact email placeholder to FAQ card`

### Task 3: EXP-05 — Add Accessible Labels to Progress Bar
**File:** `src/pages/sessions/[...slug].astro`
**Action:** Add `aria-label` attribute to each `<a>` in the progress bar `.map()` expression
**Label format:** `Session {n}으로 이동` (non-current) / `Session {n} (현재 세션)으로 이동` (current)
**Keep:** Existing `title` attribute as tooltip fallback
**Commit:** `style: EXP-05 -- add aria-label to session progress bar links`

## Verification

- `npm run build` after each task (catches template/schema errors)
- Visual check of `/season2/`, `/faq/`, and any `/sessions/session-*/` page after all tasks
- No automated test framework — manual verification

## Risks

- **LOW:** EXP-02 placeholder email won't work as a real link — accepted, user will replace
- **NONE:** All other changes are deterministic HTML/CSS edits with no side effects
